# Vertical-slice density

One small project deliberately combines graph sharing, recursion, overloads,
control-flow narrowing, and configured truncation pressure so their interaction
can become a high-value golden case.
